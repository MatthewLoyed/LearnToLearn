/**
 * Main utils export
 * Re-exports commonly used utilities for backward compatibility
 */

export { cn } from './validation/utils';
export { formatDate, truncateText, generateId, debounce, isValidEmail, capitalizeFirst, slugify } from './validation/utils';
